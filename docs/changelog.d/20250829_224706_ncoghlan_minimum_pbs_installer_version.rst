Fixed
-----

- A minimum `pbs-installer==2025.8.27` version is now specified. This ensures
  that the smaller CPython base runtime installations that omit debug
  symbols are consistently preferred (reported in :issue:`242`).

